apiVersion: duck/v1
kind: RowFilterList
filters:
  - name: us_only
    description: Analysts see only US records
    filter_sql: "region = 'US'"
    bindings:
      - principal: movie_analysts
        principal_type: group
