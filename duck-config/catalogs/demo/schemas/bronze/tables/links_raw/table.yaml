apiVersion: duck/v1
kind: Table
metadata:
  name: links_raw
  deletion_protection: false
spec:
  table_type: MANAGED
  comment: "Raw MovieLens external link references"
  columns:
    - name: movie_id
      type: BIGINT
    - name: imdb_id
      type: VARCHAR
    - name: tmdb_id
      type: BIGINT
    - name: ingested_at
      type: TIMESTAMP
